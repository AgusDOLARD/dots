#!/bin/bash

cmd_list="cancel suspend poweroff reboot"

case "$(echo "$cmd_list" | sed 's/ /\n/g' | dmenu)" in
    poweroff) systemctl poweroff ;;
    suspend) systemctl suspend ;;
    reboot) systemctl reboot ;;
    *) ;;
esac
